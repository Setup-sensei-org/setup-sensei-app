# Setup Sensei App

**Setup:**
If this project uses any API keys or secrets, copy `.env.example` to `.env` in the project root and fill in your own values before running locally. Never commit real secrets to git.

For backend setup, see `BackendCode/IMU_processing/README_decodeIMU.md` for additional instructions.
